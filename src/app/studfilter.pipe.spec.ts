import { StudfilterPipe } from './studfilter.pipe';

describe('StudfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new StudfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
