import { CreditcardvalidDirective } from './creditcardvalid.directive';

describe('CreditcardvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditcardvalidDirective();
    expect(directive).toBeTruthy();
  });
});
